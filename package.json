{
  "name": "steam-avatar",
  "description": "Fetches a steam avatar using a Steam WEB API key",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "micro",
    "dev": "concurrently \"tsc -w -p tsconfig.json \"  \"micro-dev\"",
    "test": "jest",
    "precommit": "npm test"
  },
  "dependencies": {
    "axios": "^0.19.0",
    "micro": "^9.3.4"
  },
  "devDependencies": {
    "@types/jest": "^24.0.18",
    "@types/micro": "^7.3.3",
    "@types/node": "^12.7.2",
    "concurrently": "^4.1.2",
    "jest": "^24.9.0",
    "micro-dev": "^3.0.0",
    "ts-jest": "^24.0.2",
    "tslint": "latest",
    "tslint-config-prettier": "latest",
    "typescript": "latest"
  }
}
